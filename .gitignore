# General
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE / Editor specific
.idea/
.vscode/
*.swp
*~
*.sublime-workspace
*.sublime-project

# Operating System files
Thumbs.db
ehthumbs.db

# Rust / Cargo (backend)
/target/
*.rs.bk
# Cargo.lock is often committed for binary projects to ensure reproducible builds,
# so it's not ignored by default. Add if you prefer to ignore it.

# Node.js / npm / yarn (frontend)
/node_modules/
/dist/
/.vite/
.eslintcache
coverage/

# Environment variables
.env
.env.local
.env.*.local
*.env

# Build output common names
build/
out/ wolfram-sim-rust/saved_hypergraphs/
